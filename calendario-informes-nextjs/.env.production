# Produccion: URLs relativas para que funcionen desde cualquier IP
# (LAN 192.168.x.x, Tailscale 100.x.x.x, etc.)
# Django y Next.js corren bajo el mismo origen gracias a Nginx.
NEXT_PUBLIC_DJANGO_URL=
NEXT_PUBLIC_API_URL=/registros/correspondencia
