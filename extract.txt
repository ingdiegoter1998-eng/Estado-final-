            {% comment %} HISTORIAL {% endcomment %}
            <div class="card shadow-sm mb-4">
                <div class="card-header">Historial de Eventos</div>
                <div class="card-body" style="max-height: 400px; overflow-y: auto;">
                    <div class="timeline">
                        {% for evento in historial %}
                        <div class="timeline-item">
                            <div class="timeline-marker bg-{% if evento.evento == 'RADICACION' %}primary{% elif evento.evento == 'DISTRIBUCION' %}info{% elif evento.evento == 'LECTURA' %}success{% elif evento.evento == 'RESPUESTA' %}warning{% else %}secondary{% endif %}"></div>
                            <div class="timeline-content">
                                <div class="timeline-header">
                                    <span class="badge bg-{% if evento.evento == 'RADICACION' %}primary{% elif evento.evento == 'DISTRIBUCION' %}info{% elif evento.evento == 'LECTURA' %}success{% elif evento.evento == 'RESPUESTA' %}warning{% else %}secondary{% endif %}">
                                        {{ evento.get_evento_display }}
                                    </span>
                                    <small class="text-muted ms-2">{{ evento.fecha_hora|date:"Y-m-d H:i:s" }}</small>
                                </div>
                                <div class="timeline-body">
                                    <small class="text-muted">Por: {{ evento.usuario.username|default:"Sistema" }}</small>
                                    {% if evento.descripcion %}
                                    <p class="mb-0 mt-1">{{ evento.descripcion|linebreaksbr }}</p>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                        {% endfor %}
                    </div>
                </div>
            </div>

            {% comment %} ESTADO DE LECTURA {% endcomment %}
            <div class="card shadow-sm mb-4">
                <div class="card-header">Estado de Lectura</div>
                <div class="card-body" style="max-height: 300px; overflow-y: auto;">
                    {% if usuarios_que_leyeron %}
                        <p>Leído por:</p>
                        <ul class="list-group list-group-flush">
                            {% for usuario in usuarios_que_leyeron %}
                                <li class="list-group-item">
                                    <i class="bi bi-check-circle-fill text-success me-2"></i>{{ usuario.get_full_name|default:usuario.username }}
                                </li>
                            {% endfor %}
                        </ul>
                    {% else %}
                        <p class="text-muted text-center"><em>Aún no ha sido leído por ningún destinatario.</em></p>
                    {% endif %}
                    {% with todos_destinatarios=correspondencia.distribuciones_internas.all %}
                        {% if todos_destinatarios %}
                        <hr>
                        <p>Pendiente por leer:</p>
                        <ul class="list-group list-group-flush">
                            {% for distribucion in todos_destinatarios %}
                                {% if not distribucion.leido %}
                                <li class="list-group-item">
                                    <i class="bi bi-hourglass-split text-warning me-2"></i>{{ distribucion.usuario_asignado.get_full_name|default:distribucion.usuario_asignado.username }}
                                </li>
                                {% endif %}
                            {% endfor %}
                        </ul>
                        {% endif %}
                    {% endwith %}
                </div>
            </div>

            {% if accesos_oficinas %}
            <div class="card shadow-sm mb-4">
                <div class="card-header">Oficinas con Acceso de Solo Lectura</div>
                <div class="card-body">
                    <ul class="list-group list-group-flush">
                        {% for acceso in accesos_oficinas %}
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span><i class="bi bi-building me-2 text-primary"></i>{{ acceso.oficina.nombre }}</span>
                            {% if acceso.leido %}
                                <span class="badge bg-success">Visto</span>
                            {% else %}
                                <span class="badge bg-secondary">Pendiente</span>
                            {% endif %}
